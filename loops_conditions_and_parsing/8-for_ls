#!/usr/bin/env bash
# This script lists files in the current directory, showing only the part after the first dash

for file in *; do
  # Skip hidden files
  if [[ "$file" != .* ]]; then
    # Extract part after first dash (if any)
    echo "${file#*-}"
  fi
done
